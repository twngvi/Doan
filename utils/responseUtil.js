/**
 * responseUtil.js - Response Standardization Utility
 *
 * Tác dụng:
 * - Chuẩn hóa format response JSON cho toàn hệ thống
 * - Xử lý error responses và success responses
 * - Status codes management
 * - Logging responses cho debugging
 * - Compression và optimization
 *
 * Các function chính:
 * - successResponse(): Format success response
 * - errorResponse(): Format error response
 * - paginationResponse(): Response có phân trang
 * - validationErrorResponse(): Response lỗi validation
 * - notFoundResponse(): Response not found
 * - unauthorizedResponse(): Response unauthorized
 */
